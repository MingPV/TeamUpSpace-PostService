# Gateway Configuration
GATEWAY_PORT=8080
GRPC_SERVER_ENDPOINT=localhost:50066

# Post Service Configuration
POST_SERVICE_ADDR=localhost:50066

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=postservice
